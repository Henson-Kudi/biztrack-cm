$kind: http-request
url: "{{baseUrl}}/plans/select"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "plan": "FREE"
    }
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 2000
