$kind: http-request
url: "{{baseUrl}}/auth/logout"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 15000
