$kind: http-request
url: "{{baseUrl}}/products/{{productId}}/images?page=1&limit=20"
method: GET
queryParams:
  page: "1"
  limit: "20"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 1000
